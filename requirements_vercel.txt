Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
google-generativeai==0.8.3
python-dotenv==1.0.1
psycopg2-binary==2.9.9
Werkzeug==3.0.1
PyPDF2==3.0.1
requests==2.31.0
nltk==3.8.1
# Minimal dependencies for Vercel deployment
# Heavy packages excluded to reduce bundle size:
# - pandas (use CSV module for exports instead)
# - PyMuPDF (large binary, use PyPDF2 for text PDFs only)
# - Pillow (only needed for advanced image processing)
# - EasyOCR (500MB+ models, use cloud OCR service)
# - pytesseract (requires system binaries)
# - openpyxl (only needed for XLSX export, use CSV instead)
