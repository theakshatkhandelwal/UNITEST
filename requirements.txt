# Vercel-optimized requirements (under 50MB limit)
# Excludes heavy packages: EasyOCR (~500MB), PyMuPDF, pandas, pytesseract

# Core Flask dependencies
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
WTForms==3.1.1
Werkzeug==3.0.1

# Google AI
google-generativeai==0.8.3

# Database
psycopg2-binary==2.9.9
python-dotenv==1.0.1

# HTTP requests (for external APIs like Piston/Judge0)
requests==2.31.0

# PDF processing (text-based only, lightweight)
PyPDF2==3.0.1

# NLTK (downloads data at runtime, lightweight package)
nltk==3.8.1

# Note: The following are EXCLUDED due to size:
# - easyocr (~500MB models)
# - PyMuPDF (large binary)
# - pandas (large, use csv module instead)
# - openpyxl (only needed for XLSX export)
# - pytesseract (requires system binaries)
# - pdf2image (requires Poppler system binary)
# - Pillow (only for advanced image processing)

# Features that will work:
# ✅ All quiz features (MCQ, Subjective, Coding)
# ✅ Text-based PDF processing
# ✅ CSV export (using built-in csv module)
# ✅ All coding question features
# ✅ User authentication and dashboard

# Features that will be limited:
# ⚠️ Image-based PDF processing (will show error message)
# ⚠️ XLSX export (will redirect to CSV)
